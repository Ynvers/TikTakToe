package jeu;

public class Interface {
    
    
}

/* 
    public static void Interface() {
        JFrame frame = new JFrame("Accueil");
        frame.setSize(501, 301);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        
        JPanel panel = new JPanel();

        JButton play = new JButton("PLAY");
        panel.add(play);


        ImageIcon icon = new ImageIcon("ressources/img/play.jpg");
        Image image = icon.getImage();
        Image newImage = image.getScaledInstance(20, 20, Image.SCALE_SMOOTH);
        icon = new ImageIcon(newImage);
        JLabel play_icon = new JLabel(icon);
        panel.add(play_icon);

        JLabel bienvenue = new JLabel("Bienvenue dans l'Ynvers TikTakToe");

        JTextField textField = new JTextField(20);    
        panel.setBackground(Color.DARK_GRAY);
        panel.add(bienvenue);
        panel.add(textField);
        frame.add(panel);
        frame.setVisible(true);
    }
    */